name: endec_binary
description: An unspecified binary serialization format for endec
version: 0.1.0
repository: https://github.com/gliscowo/endec.dart

resolution: workspace

environment:
  sdk: ^3.6.0

dependencies:
  endec: ^0.1.0

dev_dependencies:
  lints: ^4.0.0
